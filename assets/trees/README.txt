Individual sprites cut from treesandstufftransparent.png.
Each PNG has a real alpha channel (transparent outside the art).

Re-build:
  python tools/split_trees_spritesheet.py

Naming:
  tree_large_01..05   — top row (big trees)
  tree_small_01..05   — second row
  stumps_log_01..04   — stumps and fallen log
  bushes_rocks_01..12 — bushes and rocks
